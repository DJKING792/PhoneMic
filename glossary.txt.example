# TypMic 术语表 —— 把本文件复制为同目录的 glossary.txt 即可生效（重启服务后加载）。
#
# 作用：修正语音识别里常被听错的产品名 / 人名 / 英文工具名。
# 每行一条规则，两种写法：
#
#   错词 => 正词      （做文本替换，大小写不敏感，识别后立刻纠正，最稳）
#   词               （单独一个词 = 告诉 AI 润色优先用这个写法；仅在开启润色时生效）
#
# 以 # 开头的是注释，空行忽略。
#
# —— 示例（按需修改/删除）——

# 常见英文工具/产品名被听成中文谐音，用 => 直接纠正：
klawd => Claude
科沃斯 => Cursor
type mic => TypMic
唉批爱 => API
git hub => GitHub

# 只写标准词（不带 =>）：作为术语偏好，提示润色模型优先采用这些写法：
TypMic
Cursor
Claude
Volcengine
faster-whisper

# 离线模式（本地 faster-whisper 识别）可选依赖。
# 默认不安装，仅当你要启用 TYPOMIC_ASR=whisper 时才需要：
#     pip install -r requirements-offline.txt
# 首次运行会自动下载 Whisper 模型权重（约几百 MB，仅一次）。
faster-whisper>=1.0

# 离线模式（本地 SenseVoice 识别）可选依赖。
# 仅当你要启用 TYPOMIC_ASR=sensevoice 时才需要：
#     pip install funasr modelscope
#     pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
# 首次运行会自动从 ModelScope 下载 SenseVoiceSmall 等模型（约 1GB，仅一次）。
# 注意：funasr 的 AutoModel 依赖 torch，必须装；上面那行是纯 CPU 版 wheel，
# 不拉 CUDA 大包（约省几百 MB）。有 NVIDIA 显卡可改用默认 PyPI 的 torch（含 CUDA）以加速。
# funasr 锁定 >=1.3.19：官方明确版本，保证 rich_transcription_postprocess / generate 参数稳定。
funasr>=1.3.19
modelscope
torch
torchaudio
